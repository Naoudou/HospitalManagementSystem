@model IEnumerable<HospitalManagementSystem.Models.Nature>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Libelle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Libelle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdNature }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdNature }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdNature })
        </td>
    </tr>
}

</table>
