@model IEnumerable<HospitalManagementSystem.Models.Configuration>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Libelle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nature.Libelle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Libelle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nature.Libelle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdConfiguration }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdConfiguration }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdConfiguration })
        </td>
    </tr>
}

</table>
